# LLM API Configuration
# Optional: If not provided, system will use a mock LLM provider
GEMINI_API_KEY=

# Database Configuration (optional - defaults are usually fine)
# DATABASE_URL=sqlite:///./charcentric.db

# Kafka Configuration (optional - defaults are usually fine)
# KAFKA_BOOTSTRAP_SERVERS=kafka:9092

# Frontend API URL (optional - defaults are usually fine)
# VITE_API_URL=http://localhost:8000
